# Velorah® Hero

Single-page cinematic hero built with React, Vite, TypeScript, Tailwind CSS, and the shadcn/ui Button primitive.

## Run

```bash
npm install
npm run dev
```

Build for production with:

```bash
npm run build
```

The hero references the supplied CloudFront MP4 directly and imports Instrument Serif + Inter from Google Fonts.
